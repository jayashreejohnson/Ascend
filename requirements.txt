# Runtime deps for the agent backend (the /negotiate/stream service).
# The streaming endpoint is DB-free; SQLAlchemy/asyncpg are only imported at
# module load and never connect, so no database is required to run it.
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
sqlalchemy>=2.0
asyncpg>=0.29
python-dotenv>=1.0
httpx>=0.27
anthropic>=0.28
